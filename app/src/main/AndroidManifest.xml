<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.proyecto.DescuentosYa">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.DescuentosYa.Launcher" tools:targetApi="31">

    <!-- Clave de API para Google Maps -->
    <meta-data
        android:name="com.google.android.geo.API_KEY"
        android:value="AIzaSyCePVV5wQ5TxhxnThEmJRrGVPXtLtd3zh0" />

    <!-- SplashActivity, lanzador de la app -->
        <activity
            android:name="com.proyecto.Descuentosya.ui.theme.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.DescuentosYa.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        <!-- Deep link para billeteras -->
        <intent-filter>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data
                android:scheme="descuentosya"
                android:host="billetera_detalle"
                android:pathPrefix="/" />
        </intent-filter>
    </activity>

    <!-- MainActivity declarada para que pueda ser abierta -->
    <activity
        android:name="com.proyecto.Descuentosya.MainActivity"
        android:exported="true" />

    </application>
    </manifest>
